## Development

When starting the dev server, prefer:

```
npm run dev
```

Astro also supports background mode via `astro dev --background` (stop / status / logs with the matching `astro dev` subcommands).

Follow `AGENTS.md` for design and language rails. Full Astro docs: https://docs.astro.build
